# Public User Deck Corpus

This corpus contains small public DEVSIM Python decks that mimic common user-deck shapes:

- function-wrapped configuration;
- package imports and local overrides;
- multi-sweep bias configuration.

Each deck is executable by itself and is also used by `long_run_validation` to verify deck IR extraction, semantic patching, direct execution, benchmark artifacts, and diff lineage.

